class roomFile extends Blob {
    // Puedes agregar propiedades o métodos adicionales si lo necesitas
    constructor(parts: BlobPart[], options?: BlobPropertyBag) {
      super(parts, options);
    }
}